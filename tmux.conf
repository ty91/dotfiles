bind r source-file ~/.tmux.conf \; display "Config reloaded..."
# ===========================
# Set prefix to ctrl+e
# ===========================
unbind C-b
set -g prefix C-e

# ============================
# Binds
# ============================
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind \\ split-window -h -c '#{pane_current_path}'
bind - split-window -v -c '#{pane_current_path}'
bind v new-window -c '#{pane_current_path}'

# ============================
# Mouse
# ============================
set -g mouse on

# ============================
# Pane border
# ============================
set -g pane-border-status top
set -g pane-border-format '#{?@tmux_agent_sidebar,,#{?pane_active,#[reverse],}#{pane_index}#[default] "#{pane_title}"}'
bind t command-prompt -p "Pane title:" "select-pane -T '%%'"

# etc
set -g extended-keys always
set -g extended-keys-format csi-u
set -as terminal-features ',xterm-ghostty:extkeys'
set -g focus-events on
set -g allow-passthrough on

# tmux-agent-sidebar
set -g @tmux_agent_sidebar_auto_rename on
set -g @tmux_agent_sidebar_width 38
run-shell '/Users/taeyoung/Developer/playground/tmux-agent-sidebar/tmux-agent-sidebar.tmux'
